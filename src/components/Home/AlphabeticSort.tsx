// import type { SearchParams } from '../../types/moviesTypes'
// import Icon from '../Icon/Icon'

// export interface SearchParamsProps {
// 	searchData: SearchParams
// 	setSearchParams: React.Dispatch<React.SetStateAction<SearchParams>>
// }

// const AlphabeticSort = ({ searchData, setSearchParams }: SearchParamsProps) => {
// 	const handleToggleOrder = () => {
// 		setSearchParams((prev) => ({
// 			...prev,
// 			order: prev.order === 'ASC' ? 'DESC' : 'ASC',
// 		}))
// 	}

// 	return (
// 		<Icon id={searchData.order.toLowerCase()} onClick={handleToggleOrder} />
// 	)
// }

// export default AlphabeticSort
